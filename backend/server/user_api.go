package server

import (
	"net/http"
)

// UserAPIHandler handles user-related API endpoints.
func UserAPIHandler(w http.ResponseWriter, r *http.Request) {
	// TODO: implement user API logic (register, login, profile, settings)
}
